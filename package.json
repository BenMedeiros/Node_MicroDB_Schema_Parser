{
  "name": "node_microdb_schema_parser",
  "version": "1.0.0",
  "description": "Import data from file, parse csv/json/tab/xml then map to schema.  Find schema matches, and apply data normalization and string encoding normalization to see what type of compression/performance changes it allows for.",
  "main": "index.js",
  "scripts": {
    "test": "index.js"
  },
  "keywords": [
    "import",
    "schema-parser"
  ],
  "author": "Ben Medeiros",
  "license": "ISC",
  "dependencies": {
    "csv-parser": "^3.0.0",
    "xml2js": "^0.6.2"
  }
}
